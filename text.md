Given an array of shopping cart items:

Each item has:

name
price
quantity
category

Write a function that returns:

the total cost of the cart,
the most expensive item,
and the total number of items in each category.